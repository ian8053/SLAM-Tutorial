# CLAUDE.md

## Git Commit 規則

- **不要帶 Claude 標記**：所有 commit 都不要加 `Co-Authored-By: Claude` 和 `🤖 Generated with Claude Code`
- Commit message 保持簡潔，只描述做了什麼

## Firecrawl 使用規則

- **先確認 URL 有效**：使用 Firecrawl 前，先用 `WebFetch` 確認網頁不是 404
- 每次 scrape = 1 credit，即使失敗也會扣
- 免費方案總共 525 credits
